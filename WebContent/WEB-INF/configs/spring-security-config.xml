<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security-3.1.xsd">
    <http auto-config="true" use-expressions="true">
    	<intercept-url pattern="/" access="permitAll" />
    	<intercept-url pattern="/login" access="isAnonymous()" />
		<intercept-url pattern="/admin" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/welcome" access="isAuthenticated()" />

		<!-- Access denied page -->
		<access-denied-handler ref="myAccessDeniedHandler" />
		<form-login 
		    login-page="/login" 
			username-parameter="email"
			password-parameter="password"
			authentication-failure-url="/login?error"
			authentication-success-handler-ref="customAuthenticationSuccessHandler" />
		<logout logout-success-url="/login?logout" />
		
	</http>
	<beans:bean id="customAuthenticationSuccessHandler" 
		class="framgiavn.project01.web.security.CustomAuthenticationSuccessHandler" />
	
	<beans:bean id="AccountDetails" parent="transactionProxyTemplate">
		<beans:property name="target">
			<beans:bean class="framgiavn.project01.web.security.AccountDetails">
				<beans:property name="userDAO" ref="UserDAO" />
			</beans:bean>
		</beans:property>
	</beans:bean>	
	
	<beans:bean id="myAccessDeniedHandler"
		class="framgiavn.project01.web.security.MyAccessDeniedHandler">
	</beans:bean>
	
	<beans:bean id="bcrypt" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
		
	<authentication-manager>
		<authentication-provider user-service-ref="AccountDetails">
			<password-encoder ref="bcrypt" />
		</authentication-provider>    
	</authentication-manager>
</beans:beans>
